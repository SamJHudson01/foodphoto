# Council Implementation Log: Hosted Storage + Council Stack Migration

**Plan:** Hosted Storage + Council Stack Migration
**Tasks completed:** Core implementation complete; browser smoke blocked by missing Clerk and R2 write credentials.
**Conventions respected:** Next.js App Router, TypeScript strict, CSS Modules, Prisma migration generated from schema, Clerk `proxy.ts` + `Show` components.

---

## Task Log

### Task 1: Establish Authenticated App Shell

**Domain:** Troy Hunt x Carmack - Broken Access Control
**Ref applied:** Middleware is UX gating only; data operations verify Clerk user id in tRPC.

**Files changed:**
- `proxy.ts` - added `clerkMiddleware()` with the `__clerk` matcher.
- `app/layout.tsx` - moved `ClerkProvider` inside `<body>` and added app providers.
- `app/page.tsx` - gated signed-in app and signed-out sign-in UI with `<Show>`.

**Verification:** `npm run build` passed.
**Notes:** Clerk env keys still need to be added outside source control.

---

### Tasks 2-3: Prisma + Neon Foundation and Ownership

**Domain:** Brandur Leach x Carmack - Constraints are assertions
**Ref applied:** User-owned records, FK constraints, unique constraints, timezone-aware timestamps, indexed owner/date queries.

**Files changed:**
- `prisma/schema.prisma` - added `User`, `FoodEntry`, and `DailyRoundup`.
- `prisma.config.ts` - added Prisma 7 config loading `.env.local`.
- `lib/prisma.ts` - added Prisma 7 `PrismaPg` adapter.
- `prisma/migrations/20260530165245_init_hosted_storage/migration.sql` - generated by Prisma from the schema.

**Verification:** `npm run db:validate`, `npx prisma migrate status`, `npx tsc --noEmit`, and `npm run build` passed.
**Notes:** The initial hand-written migration was deleted; Prisma generated and applied the committed migration.

---

### Tasks 4-7: tRPC, R2 Adapter, and Entry CRUD

**Domain:** Matteo Collina x Carmack - Auth as a type constraint
**Ref applied:** All data procedures use `protectedProcedure`, Zod validation, and authenticated user filtering.

**Files changed:**
- `server/trpc.ts` - created tRPC context, router helpers, protected procedure, and sanitized internal errors.
- `server/routers/_app.ts` - created domain router composition.
- `server/routers/entries.ts` - added list/create/update/delete with user ownership checks.
- `lib/r2.ts` - added server-only R2 put/delete/public URL helpers.
- `app/api/trpc/[trpc]/route.ts` - added App Router tRPC route.
- `app/trpc.ts`, `app/trpc-provider.tsx` - added React Query/tRPC client provider.

**Verification:** `npx tsc --noEmit` and `npm run build` passed.
**Notes:** Actual photo upload needs `CLOUDFLARE_R2_ACCESS_KEY_ID` and `CLOUDFLARE_R2_SECRET_ACCESS_KEY` in Vercel/local env.

---

### Tasks 8-10: Server State Gallery and Local Migration

**Domain:** Martin Fowler x Carmack / Kent C. Dodds x Carmack
**Ref applied:** Server state is owned by tRPC/TanStack Query; IndexedDB remains only a migration source.

**Files changed:**
- `app/food-photo-app.tsx` - replaced active local CRUD with tRPC queries/mutations, added IndexedDB migration banner, preserved local photos after confirmed upload, and kept camera compression.
- `app/page.module.css` - added migration banner styling.

**Verification:** `npx tsc --noEmit` and `npm run build` passed.
**Notes:** The large component was not fully split into separate files yet; behavior boundaries are clearer, but cosmetic file extraction remains deferred.

---

### Tasks 11-13: DB-Backed Roundups, Payload Bounds, Security Headers

**Domain:** Troy Hunt x Carmack / Vercel Performance
**Ref applied:** Roundups send only the selected day, use R2 URLs server-side, and store generated output in Postgres.

**Files changed:**
- `server/routers/roundups.ts` - added DB-backed OpenRouter generation and roundup persistence.
- `app/api/roundup/route.ts` - removed the old direct local-blob route.
- `next.config.ts` - added CSP, referrer policy, nosniff, HSTS, and camera-scoped Permissions-Policy.

**Verification:** `npx tsc --noEmit` and `npm run build` passed.
**Notes:** CSP includes Clerk and R2 origins; OpenRouter remains server-side only.

---

### Task 14: Focused Verification

**Domain:** Kent C. Dodds x Carmack - Test behaviour, not implementation
**Ref applied:** Ran static, migration, and production build checks; recorded browser smoke blockers.

**Verification:**
- `npm run db:validate` - passed.
- `npx prisma migrate status` - passed; Neon is up to date.
- `npx tsc --noEmit` - passed.
- `npm run build` - passed.

**Notes:** No integration test harness exists in the repo yet. Full browser smoke requires Clerk keys plus R2 write credentials.

---

## Watchpoints Addressed

- Data operations filter by Clerk user id through protected tRPC procedures.
- Photos are uploaded server-side to R2; browser never receives storage credentials.
- OpenRouter receives only selected-day images/notes.
- Existing local IndexedDB photos are not silently discarded.
- Generated local artifacts are ignored via `.gitignore`.

## Pre-existing Issues Encountered

- `next-env.d.ts` had previously been dirtied by Next generated type changes; it was not changed here.
- `.env.local` did not include Neon/R2 values, so Prisma initially fell back to the config placeholder before env setup.
- `npm` reports an existing `tokenHelper` warning from user npm config.

## Ready for Review

Implementation log path:
`.council/implement-log/2026-05-30-hosted-storage-council-stack.md`

Files in scope:
- `app/food-photo-app.tsx`
- `app/layout.tsx`
- `app/page.tsx`
- `app/page.module.css`
- `app/trpc.ts`
- `app/trpc-provider.tsx`
- `app/api/trpc/[trpc]/route.ts`
- `server/trpc.ts`
- `server/routers/_app.ts`
- `server/routers/entries.ts`
- `server/routers/roundups.ts`
- `lib/prisma.ts`
- `lib/r2.ts`
- `prisma/schema.prisma`
- `prisma.config.ts`
- `prisma/migrations/20260530165245_init_hosted_storage/migration.sql`
- `proxy.ts`
- `next.config.ts`
